const prompt = require('prompt-sync')()

/* Construir um programa que leia uma string s, e dois
caracteres a e b. Em seguida, o programa deve substituir
todas as ocorrências do caractere a na string s pelo 
caractere b.
*/

function main() {
    const s = Number(prompt('Digite aqui uma letra com valor de texto: '))
    const a = Number(prompt('Digite aqui uma letra com valor de texto: '))
    const b = Number(prompt('Digite aqui uma letra com valor de texto: '))
}
    const vetor_a = Array(s)
    
    for (let i = 0; i < s; i++) {
        vetor_a[i] = Number(prompt(`Inversão de valores de uma vetor para uma string ${i}: `))
    }
    const vetor_a = Array(b)

    for (let i = 0; i < b; i++) {
        vetor_a[i] = Number(prompt(`Incrementando valor do caractere a para a caractere b ${i}: `))
    }

    let j = vetor_a.lenght - 1
    for (item of vetor_a) {
        vetor_a[j] = item
        j++
    }

    if ((charAt(i) += nova_frase) {
        vetor_a[i] = vetor_b.lenght - 1
    }else {
        console.log('FIM.')
    }
    
}

main()


