const prompt = require('prompt-sync')()

/* Escreva uma função que dados 2 valores reais p e x calcule e
retorne quanto vale p por cento de x.
*/

function main(){
    const p = Number(prompt('Digite o primeiro número real: '))
    const x = Number(prompt('Digite o segundo número real: '))

    console.log(nova_nomenclatura(nomenclatura));
    let p, x
    contador = 0;
    p = parseFloat.Number
    x = parseFloat.Number

    for (let i = 0; double_parseFloat.Number <= 1; i++) {
    console.log(i)
    }

    console.log(`O valor de x por cento de p valerá ${i}: `)
    return 0;

}

main()
